# Parlour Booking & Management

Development checkpoint recovered from the implementation session. The Java Spring Boot backend and proposal-aligned MySQL schema are present. The native Android preview, admin UI, build workflow and complete setup guide are being added in a following commit.

This is not a production deployment. Six local backend integration tests passed before the build environment became unavailable. The source was recovered from the session after that interruption and must also pass the repository build. Firebase Console and AI Studio could not be accessed due to gateway errors. No live Firebase settings, payment credentials or hosting have been configured.

All business data is user-entered. Migrations do not seed sample parlours or bookings. The optional administrator bootstrap is driven by environment values.
